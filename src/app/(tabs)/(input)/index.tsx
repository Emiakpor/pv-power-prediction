import { defaultStyles } from "@/styles"
import { Text, View } from "react-native"

const InputScreen = () => {
    return (
        <View style={defaultStyles.container}>
            <Text style={defaultStyles.text}>Input Parameter</Text>
        </View>
    )
}

export default InputScreen